Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-Mail==0.9.1
python-dotenv==1.0.1
gunicorn==21.2.0
psycopg2-binary==2.9.9  # If using PostgreSQL on RDS

# Optional: If using MySQL instead of PostgreSQL
# pymysql==1.1.0

# Recommended for production:
Werkzeug==3.0.1  # Flask dependency, pin to stable version
Jinja2==3.1.3    # Flask templates
itsdangerous==2.1.2  # Flask security token dependency
click==8.1.7     # Flask CLI dependency

# Recommended: Env var manager
python-decouple==3.8  # Optional, but handy for config

# Recommended: Production logging
loguru==0.7.2   # Modern logging

# Optional: If you use AWS S3 for static/media storage
boto3==1.34.107

# Optional: If you plan to run Celery tasks
# Celery==5.3.6
# redis==5.0.3

# Optional: If you want to use dotenv safely with Gunicorn
python-dotenv==1.0.1

# Testing tools (optional for dev only)
pytest==8.1.1
pytest-flask==1.3.0
